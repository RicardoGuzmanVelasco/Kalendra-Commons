using UnityEngine;

namespace Kalendra.Commons.Tests.Runtime
{
    public class DummyScript : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
